# 服务器部署手册（含域名访问）

> 把一个网页程序部署到服务器上，并用**域名**打开它。本手册以当前这套系统为真实范例，换项目只改开头"填空表"里的值即可。
> 想了解"改完代码怎么自动上线"，看另一份《自动部署说明.md》。

---

## 一、开始前先填空

| 占位符 | 含义 | 本项目真实值（范例） | 你的值 |
|---|---|---|---|
| 【项目名】 | 项目代号 | marketing-platform | ______ |
| 【GitHub仓库地址】 | git@github.com:.../xxx.git | 你的私有仓库 | ______ |
| 【服务器公网IP】 | 云服务器公网 IP | 阿里云 ECS 的公网 IP | ______ |
| 【服务器登录用户名】 | SSH 用户名 | root | ______ |
| 【服务器上的目录】 | 代码放哪个文件夹 | /root/marketing-platform | ______ |
| 【入口文件】 | 启动文件 | server.js | ______ |
| 【pm2进程名】 | pm2 里的进程名 | marketing-platform | ______ |
| 【端口】 | 程序监听的内部端口 | 3002 | ______ |
| 【域名】 | 访问用的域名 | pinxuan.shiganneibu.com | ______ |
| 【nginx配置文件名】 | 放在 /etc/nginx/conf.d/ 下 | pinxuan-shiganneibu.conf | ______ |

> 本项目部署在**阿里云（Alibaba Cloud）ECS**。

**先理解整条链路**（新手最容易漏掉中间两层）：

```
用户在浏览器打开 http://【域名】
        │  ①域名解析(DNS)：把【域名】翻译成【服务器公网IP】
        ▼
到达服务器的 80 端口
        │  ②nginx 反向代理：把 80 端口的请求转发给内部【端口】
        ▼
你的程序（pm2 跑在 127.0.0.1:【端口】，如 3002）
```

**很多人只做了第③层（pm2 起服务），忘了①DNS 和②nginx，结果域名死活打不开。** 本手册就是把这三层都配齐。

---

## 二、服务器上要装什么

一台能 SSH 登录的云服务器，装三样东西：

- **Node.js** — 用来跑程序。
- **pm2** — 让程序常驻后台、崩了自动拉起、服务器重启后自动启动。装法：`npm install -g pm2`
- **nginx** — 网关（反向代理），把【域名】的访问转发给你的程序。用系统的包管理器装（如 `yum install nginx` / `apt install nginx`）。

装好后 `pm2 -v`、`nginx -v` 能打印版本号就说明就绪。

---

## 三、把程序跑起来

### 3.1 先让服务器能拉取你的私有仓库（关键，最容易漏）

`git clone` / `git pull` 是"服务器 → 去 GitHub 拉代码"。**私有仓库**必须先给服务器授权，否则拉不下来。

```bash
# 在服务器上生成一把它自己的密钥（一路回车）
ssh-keygen -t ed25519 -C "server-【项目名】" -f ~/.ssh/id_ed25519
# 打印公钥，整段复制
cat ~/.ssh/id_ed25519.pub
```

把公钥加到 **GitHub 仓库 → Settings → Deploy keys → Add deploy key**（勾选只读即可）。公开仓库可跳过这步。

### 3.2 拉代码、装依赖、启动

```bash
git clone 【GitHub仓库地址】 【服务器上的目录】
cd 【服务器上的目录】
npm install
pm2 start 【入口文件】 --name 【pm2进程名】
pm2 save                       # 保存进程列表，服务器重启后自动拉起
```

**先在服务器本机自测**，确认程序活着：

```bash
curl http://127.0.0.1:【端口】     # 有返回(HTML)= 程序正常在跑
```

> 这一步通了，只代表程序活着、能用 `http://【服务器公网IP】:【端口】` 访问。**要用域名，继续往下配 nginx 和 DNS。**

---

## 四、用域名访问（第①层）：配 nginx 反向代理

程序只监听内部【端口】(如 3002)，不占 80。让用户从【域名】(80 端口)访问、由 nginx 转发到 `127.0.0.1:【端口】`。

### 4.1 写一个站点配置文件

新建文件 `/etc/nginx/conf.d/【nginx配置文件名】`，内容如下（把【】替换成你的值）：

```nginx
server {
    listen 80;
    server_name 【域名】;
    location / {
        proxy_pass http://127.0.0.1:【端口】;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
    }
}
```

### 4.2 确保 nginx 加载了 conf.d 目录

有些 nginx 主配置默认**没有**加载 conf.d 目录，那你上面写的文件就不生效。检查 `/etc/nginx/nginx.conf` 的 `http { ... }` 里有没有这一行：

```nginx
include /etc/nginx/conf.d/*.conf;
```

**没有**就在 `http {` 后面手动加上（改之前先备份）：

```bash
cp /etc/nginx/nginx.conf /etc/nginx/nginx.conf.bak
```

### 4.3 自检并生效

```bash
nginx -t          # 语法自检，必须显示 successful
nginx -s reload   # 通过后再 reload，让新配置生效、且不断线
```

> ⚠️ **一定先 `nginx -t` 通过再 reload。** 配置有语法错时直接 reload，会让 nginx 起不来，把这台服务器上**所有站点一起弄挂**。

---

## 五、用域名访问（第②层）：加 DNS 解析（A 记录）

光配 nginx 还不够——还要让【域名】能解析到你的服务器 IP：

1. 登录【域名】所在的**域名服务商 DNS 控制台**。
2. 新增一条解析记录，类型选 **A 记录**。
3. **主机记录 / 子域名**：填子域名（如 `pinxuan`，对应完整域名 `pinxuan.shiganneibu.com`）。
4. **记录值**：填【服务器公网IP】。
5. 保存，等 DNS 生效（通常几分钟）。之后用浏览器打开 `http://【域名】` 就能访问了。

> 验证是否生效：本机 `ping 【域名】`，看解析出来的 IP 是不是你的【服务器公网IP】。

---

## 六、同一台服务器跑多个站点

靠 nginx 的 **server_name（域名）** 来区分请求该由哪个站点处理。做法：

- **每个站点一个独立的** `/etc/nginx/conf.d/xxx.conf`，各自写自己的 `server_name` 和 `proxy_pass`（转发到各自的端口）。
- 新增一个站点，只是**再加一个配置文件**，照第四节做一遍即可，**不会影响服务器上原有的其它站点**。

> 本项目就是这样和服务器上其它系统共存的：它有独立的 `pinxuan-shiganneibu.conf`，只匹配自己的域名，各站互不干扰。

---

## 七、关于 HTTPS（当前是 http，浏览器会显示"不安全"）

目前【域名】只配了 **80 端口（http）**，没有配 SSL 证书，所以浏览器地址栏会提示"不安全"。要升级成带锁的 **https**：

1. 申请一张 **SSL 证书**（免费的 Let's Encrypt / certbot，或云厂商的免费证书）。
2. 在 nginx 里再加一个 `listen 443 ssl;` 的 server 块，填上证书路径，并把 80 端口跳转到 443。

这一步**目前没做**，需要时再加即可（不影响现在用 http 访问）。

---

## 八、打不开怎么排查（按顺序查）

1. **程序活着吗**：`pm2 status` 看【pm2进程名】是不是 `online`；`curl http://127.0.0.1:【端口】` 有没有返回。没有 → 看 `pm2 logs 【pm2进程名】` 找报错。
2. **域名解析生效没**：`ping 【域名】`，解析出的 IP 要等于【服务器公网IP】。不对 → DNS 的 A 记录没配好或没生效（第五节）。
3. **nginx 对不对**：`nginx -t` 是否通过；`/etc/nginx/conf.d/【nginx配置文件名】` 在不在；`nginx.conf` 里有没有 `include /etc/nginx/conf.d/*.conf;`（第四节）。
4. **端口/防火墙**：云服务器的**安全组 / 防火墙**有没有放行 **80 端口**（要用 https 还要放 443）。
5. **缓存**：换个浏览器，或强刷 `⌘/Ctrl + Shift + R`。

---

## 附：三样东西别搞混

| | 是什么 | 走不走 Git | 会不会因改代码丢失 |
|---|---|---|---|
| **代码** | 功能、页面、逻辑 | ✅ 走 Git，改了要推 | — |
| **数据** | 用户填的业务数据（存服务器数据库/文件） | ❌ 不走 Git | ❌ 不会，改代码不动数据 |
| **依赖** | node_modules 等 | ❌ 不传（服务器 `npm install` 自己装） | — |

> 数据不会因部署丢失，但**一定要做定时备份**（备份到服务器之外），防服务器整个挂掉。
