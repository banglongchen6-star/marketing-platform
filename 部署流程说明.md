# 改代码 → 上线 通用流程说明

> 一句话:**改完代码 → 传 Git → 自动部署到服务器 → 刷新网页看到新版。三步一体,少一步都不行。**
> 本文是通用模板,任何"网页程序 + 服务器"的项目都能套用。

---

## 一、整条链路长什么样

```
① 本地/远程改代码
        │
        ▼
② 提交并推送到 Git（git commit + git push）
        │
        ▼
③ GitHub Actions 自动部署（拉最新代码 → 装依赖 → 重启服务）
        │
        ▼
④ 服务器跑上新代码
        │
        ▼
⑤ 你刷新网页，看到新版本
```

**关键点:代码只有推送到 Git,服务器才会更新。** 本地改了不推 = 没改,网页永远是旧的。

---

## 二、日常怎么操作(每次改动)

1. 改代码
2. `git add` 改动的文件
3. `git commit -m "这次改了啥"`
4. `git push`
5. 等 GitHub Actions 显示"部署成功"(约 15~30 秒)
6. 刷新网页(必要时按 `Ctrl/⌘ + Shift + R` 强制刷新)

> 一般会等"部署成功"再确认,而不是推完就完事——因为部署可能失败(语法错、服务器连不上等),要盯一眼。

---

## 三、三样东西别搞混

| | 是什么 | 走不走 Git | 会不会因改代码丢失 |
|---|---|---|---|
| **代码** | 功能、页面、逻辑 | ✅ 走 Git,改了要推 | — |
| **数据** | 用户填的业务数据(存服务器数据库/文件) | ❌ 不走 Git | ❌ 不会,改代码不动数据 |
| **构建产物/依赖** | node_modules 等 | ❌ 不传(服务器自己装) | — |

---

## 四、换个新程序,怎么套用这套流程(一次性设置)

只要满足"有代码仓库 + 有一台服务器",就能配出同款自动部署。清单:

### 需要准备
- [ ] 一个 **Git 仓库**(GitHub / GitLab 等),**设为私有**(尤其打算对外卖的程序,源码别公开)
- [ ] 一台 **服务器**(阿里云/腾讯云等),能 SSH 登录
- [ ] 服务器上装好运行环境(如 Node.js)和进程管理器(如 pm2,保证程序崩了自动拉起)

### 配置自动部署(GitHub Actions)
在仓库里放一个部署配置文件 `.github/workflows/deploy.yml`,核心逻辑就三步:

```yaml
# 触发时机:推送到 main 分支就部署
on:
  push:
    branches: [main]

# 部署动作:SSH 登录服务器 → 拉代码 → 装依赖 → 重启
#   cd 到项目目录
#   git pull
#   npm install
#   pm2 restart 你的服务名
```

### 需要在仓库里设置的密钥(Settings → Secrets)
- [ ] `SSH_HOST`:服务器 IP
- [ ] `SSH_USER`:登录用户名
- [ ] `SSH_PRIVATE_KEY`:SSH 私钥(用来免密登录服务器)

> 配好后,以后每次 `git push`,GitHub 就会自动帮你部署,不用手动登服务器。

---

## 五、常见问题

**Q:我改完了怎么网页还是旧的?**
先确认:①有没有 push?②Actions 是不是"部署成功"?③浏览器缓存——按 `Ctrl/⌘ + Shift + R` 强制刷新。

**Q:部署失败了会怎样?**
服务器保持旧版本不动(不会崩),你修好再推一次即可。所以失败不可怕,别不看结果就走。

**Q:多人协作/多台设备改同一个项目?**
改之前先 `git pull` 拉最新,避免和别人的改动冲突。

**Q:数据会不会因为部署丢?**
不会。数据在服务器上独立存放,和代码部署是两回事。但**一定要有自动备份**(定时备份到服务器之外),防服务器整个挂掉。

---

## 六、给"要对外卖的程序"的额外提醒

- Git 仓库 **必须私有**,否则等于白送源码。
- 每个客户的数据**必须分开存**(一客户一套,或按账号隔离),绝不能混在一份数据里。
- 对外服务要上 **HTTPS**、密码**加密存储**、登录令牌**会过期**。
- 存了手机号/地址等个人信息,要有**隐私政策**并符合《个人信息保护法》。
