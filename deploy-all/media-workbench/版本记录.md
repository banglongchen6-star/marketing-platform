# 品宣运营工作台 - 版本记录

## 当前版本：v8 (2026-04-25)

### 本次修复
- **BUG修复**：修复了导致页面白屏的JavaScript语法错误
  - 位置：`renderTrendChart()` 函数，第1224行
  - 问题：`.filter(Boolean)]` 缺少闭合括号 `)`
  - 修复：`filter(Boolean))]` → 添加了缺失的 `)`

---

## v7 (2026-04-25)
- 数据看板时间维度新增「按月」按钮（后来回滚）
- 发货管理按钮改名为「去发货」
- 候选管理新增时间标签并按最新时间排序

## v6 (2026-04-25)
- 样品管理：总览排版优化（分区明确、字体大）
- 样品管理：搜索增加产品名称
- 发货链接跳转移到顶部
- 新增样品可删除发货链接和平台
- 总览改为：总样品数/总花费/已寄出/已签收
- 素材管理：阿里云盘链接跳转移到顶部
- 素材管理：编辑删除素材链接
- 修复新增结算不成功的bug

## v5 (2026-04-24)
- 内容排期增加事项提醒（提前三天+当天）
- 发货管理按钮改名为「去发货」
- 候选管理新增时间标签并按最新时间排序

## v4 (2026-04-24)
- 内容排期日历字体加大、字段分行显示
- 删除编辑弹窗中的内容类型字段
- 修复内容发布搜索框bug
- 候选管理：是否合作和最终结论改为行内下拉直接编辑
- 素材管理：素材类型改为发布时间、删除内容主题字段
- 达人结算：状态删除已结算选项、发票状态删除已收票选项
- 内容发布：平台改为多选复选框
- 封面图片上传功能（进行中）

## v3 (2026-04-23)
- 大规模重构
- 明亮简约风格
- 数据看板4大模块标题更醒目
- 数据看板增加时间维度选择（7天/15天/30天）
- 删除达人管理模块
- 内容发布按达人分类 + 详情弹窗（20+字段）
- 内容排期：状态改为自定义进度，日期只选到天
- 导出Excel可选时间维度
- 新增：候选管理、样品管理、素材管理、达人结算

---

## 文件路径
- 主文件：`C:\Users\Admin\.qclaw\workspace\media-workbench\index.html`
- 归档：`C:\Users\Admin\.qclaw\workspace\media-workbench_archive\`

## 演示账号
- 账号：admin
- 密码：admin123

---

## 常用工具
- **Node.js 语法检查**（在文件有改动后运行，确保无白屏）：
```powershell
$bytes = [System.IO.File]::ReadAllBytes("C:\Users\Admin\.qclaw\workspace\media-workbench\index.html")
$content = [System.Text.Encoding]::UTF8.GetString($bytes)
$scriptStart = $content.IndexOf("<script>", $content.IndexOf("<script>", $content.IndexOf("<script>") + 1) + 1) + 8
$scriptEnd = $content.LastIndexOf("</script>")
$jsCode = $content.Substring($scriptStart, $scriptEnd - $scriptStart)
$tempFile = "$env:TEMP\test_js.js"
[System.IO.File]::WriteAllText($tempFile, $jsCode, [System.Text.Encoding]::UTF8)
node --check $tempFile
```

## 归档打包命令
```powershell
$date = Get-Date -Format "yyyyMMdd_HHmmss"
$archiveDir = "C:\Users\Admin\.qclaw\workspace\media-workbench_archive"
if (!(Test-Path $archiveDir)) { New-Item -ItemType Directory -Path $archiveDir | Out-Null }
$zipPath = "$archiveDir\media-workbench_v8_$date.zip"
Compress-Archive -Path "C:\Users\Admin\.qclaw\workspace\media-workbench\*" -DestinationPath $zipPath -Force
Write-Host "归档完成: $zipPath"
```
