# 自动部署说明 — 改完代码怎么自动上线

> 一句话原理：**你把代码推到 GitHub → GitHub 自动 SSH 登录你的服务器 → 帮你拉最新代码、装依赖、重启服务 → 网页就是新版了。**
> 配好一次，以后每次改完代码只用打 3 条命令，剩下全自动，不用再手动登服务器。
> 想让"域名"能打开程序，看另一份《部署流程说明.md（服务器部署手册）》。

---

## 一、开始前先填空

下面这些是"换项目就要改"的地方，先把你自己的值填在右边，后面命令里凡是出现【方括号】的都替换成你填的值：

| 占位符 | 含义 | 你的值 |
|---|---|---|
| 【项目名】 | 项目代号，如 marketing-platform | ______ |
| 【GitHub仓库地址】 | 如 git@github.com:你的名字/仓库.git | ______ |
| 【服务器公网IP】 | 服务器的公网 IP 地址 | ______ |
| 【服务器登录用户名】 | SSH 登录用户名，如 root | ______ |
| 【服务器上的目录】 | 代码放服务器哪个文件夹，如 /root/marketing-platform | ______ |
| 【pm2进程名】 | 服务在 pm2 里的名字，如 marketing-platform | ______ |
| 【入口文件】 | 启动文件，如 server.js | ______ |
| 【端口】 | 服务监听端口，如 3002 | ______ |

> 本项目的真实值供参考：项目名 = marketing-platform，服务器目录 = /root/marketing-platform，pm2进程名 = marketing-platform，入口文件 = server.js，端口 = 3002，云主机 = 阿里云 ECS。

---

## 二、这套自动部署需要准备什么

只要三样东西，就能配出自动部署：

1. **一个 GitHub 仓库**（放你的代码），**建议设为私有**——尤其是要对外卖的程序，别公开源码。
2. **一台能 SSH 登录的服务器**（阿里云 / 腾讯云等都行），上面装好 Node.js 和 pm2（装法见《部署流程说明.md》服务器部署手册）。
3. **一把"部署专用"的 SSH 密钥**——让 GitHub 能免密码登录你的服务器。下面教你生成。

### 生成一把部署用 SSH 密钥

在你**本地电脑**（Mac 的"终端"）里执行，一路回车即可（密码留空）：

```bash
ssh-keygen -t ed25519 -C "deploy-【项目名】" -f ~/deploy_key
```

执行完会生成两个文件：

- `~/deploy_key`：**私钥**（保密，等下贴到 GitHub Secrets）
- `~/deploy_key.pub`：**公钥**（公开，等下放到服务器上，表示"信任这把钥匙"）

**把公钥装到服务器上**（让这把钥匙能登录服务器）：

```bash
ssh-copy-id -i ~/deploy_key.pub 【服务器登录用户名】@【服务器公网IP】
```

如果 `ssh-copy-id` 用不了，就手动把 `~/deploy_key.pub` 里的内容，追加到服务器的 `~/.ssh/authorized_keys` 文件末尾。

**验证这把钥匙能免密登录**（能直接进服务器就说明成了）：

```bash
ssh -i ~/deploy_key 【服务器登录用户名】@【服务器公网IP】
```

---

## 三、在 GitHub 要配置的 Secrets（3 个）

打开你的 GitHub 仓库页面 → **Settings → Secrets and variables → Actions → New repository secret**，逐个新增下面 3 个：

| Secret 名字（照抄，别改） | 里面填什么 |
|---|---|
| `SSH_HOST` | 服务器地址，填【服务器公网IP】 |
| `SSH_USER` | 登录用户名，填【服务器登录用户名】（如 root） |
| `SSH_PRIVATE_KEY` | **私钥全文**。用命令 `cat ~/deploy_key` 打印出来，把从 `-----BEGIN` 到 `-----END` 的**全部内容**（含首尾这两行）整段复制粘贴进去 |

> 只需要这 3 个。名字必须一字不差，因为部署配置文件里就是按这几个名字去读的。

---

## 四、服务器一次性初始化（只做一次）

第一次上线前，先手动登录服务器，把代码放好、装好依赖、用 pm2 起一次服务。以后就全自动了。

### 4.1 先让服务器能拉取你的私有仓库（关键，最容易漏！）

上面第二节那把钥匙，是"GitHub → 登录服务器"方向用的。而 `git clone` / 以后自动部署里的 `git pull`，是"**服务器 → 去 GitHub 拉代码**"，方向相反，需要**另一把**被 GitHub 认可的钥匙。**私有仓库**不配这步，会卡在拉代码这一步（自动部署每次 `git pull` 都失败）。

在**服务器上**执行：

```bash
# 1) 在服务器上生成一把它自己的密钥（一路回车）
ssh-keygen -t ed25519 -C "server-【项目名】" -f ~/.ssh/id_ed25519

# 2) 打印公钥，整段复制
cat ~/.ssh/id_ed25519.pub
```

把打印出来的公钥，加到 **GitHub 仓库 → Settings → Deploy keys → Add deploy key**（标题随便填，勾选只读即可）。这样服务器就有权拉这个私有仓库了。

> 公开仓库可以跳过这步；私有仓库（推荐）必须做。注意：这把"服务器上的钥匙"和第二节那把"GitHub 登录服务器的钥匙"是**两把不同的钥匙、两个方向**，别搞混。

### 4.2 拉代码、装依赖、启动

```bash
# 1. 登录服务器
ssh 【服务器登录用户名】@【服务器公网IP】

# 2. 把代码拉到指定目录
git clone 【GitHub仓库地址】 【服务器上的目录】
cd 【服务器上的目录】

# 3. 安装依赖
npm install

# 4. 用 pm2 首次启动，并起一个固定的进程名
pm2 start 【入口文件】 --name 【pm2进程名】

# 5. 保存 pm2 进程列表(让服务器重启后能自动拉起)
pm2 save
```

做完这步，服务应该已经在【端口】上跑起来了。之后的每次更新都交给 GitHub 自动完成。

---

## 五、部署配置文件模板：.github/workflows/deploy.yml

在你的项目里新建文件 `.github/workflows/deploy.yml`，把下面**整段复制**进去，再把【方括号】替换成你的值即可。它做的事就是：一旦有人推代码到 main 分支，就 SSH 登录服务器，依次拉代码、装依赖、重启服务。

```yaml
name: Deploy

on:
  push:
    branches: [main]        # 推到 main 分支就自动部署

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 15m
          script: |
            cd 【服务器上的目录】
            git pull origin main
            npm install
            pm2 delete 【pm2进程名】 2>/dev/null || true
            pm2 start 【入口文件】 --name 【pm2进程名】
            pm2 save
            echo "✅ Deploy done"
```

几个要点解释（不用背，知道就行）：

- `on.push.branches: [main]`：**只有推到 main 分支才触发**。推到别的分支不会部署。
- `pm2 delete ... || true`：先删掉旧进程；即使没这个进程也不报错。这样能保证是"干净重启"跑最新代码。
- `pm2 start ... --name 【pm2进程名】`：用固定名字重新启动。
- `command_timeout: 15m`：整个部署最多跑 15 分钟，超时才算失败。

> ⚠️ 只用 pm2 起服务，程序是跑在【端口】(如 3002)上的，这时只能用 `http://服务器IP:端口` 访问。**想用"域名"打开，必须再配一层 nginx 反向代理**（把域名的 80 端口转发到内部【端口】）——完整做法见《部署流程说明.md》服务器部署手册。
> 本项目图省事，把这段 nginx 配置也写进了上面的部署脚本里，每次部署自动确保生效；你的新项目手动配一次也完全可以。

---

## 六、日常怎么用：改完代码只要 3 条命令

配好之后，以后每次改动只需要：

```bash
git add .
git commit -m "这次改了啥"
git push
```

推完就不用管了。GitHub 会自动帮你部署，**大约 1–3 分钟**后（要装依赖、重启，网络慢时更久），刷新网页就是新版。

**看部署有没有成功**：打开 GitHub 仓库页面 → 顶部 **Actions** 标签 → 最上面那条就是刚才这次。绿色勾 ✅ = 成功，红色叉 ❌ = 失败（点进去能看到哪一步出错）。

> 建议每次 push 后瞄一眼 Actions 是不是绿的，别推完就走——万一失败了你以为上线了其实没有。
> 部署失败不可怕：**失败时服务器保持旧版本继续跑，不会崩**，你修好再推一次就行。

---

## 七、常见问题排查

**Q：我推了代码，网页怎么还是旧的？**
按顺序查三步：
1. **真的 push 了吗？** 本地 `git status` 看看是不是还有没提交/没推的改动。
2. **Actions 成功了吗？** 去 GitHub 的 Actions 页看最新那条是不是绿勾。红叉就点进去看报错。
3. **是浏览器缓存吗？** 按 `⌘ + Shift + R`（Mac）或 `Ctrl + Shift + R`（Win）强制刷新。

**Q：Actions 绿勾了，可域名/网页还是打不开？**
这多半是"域名/nginx"层的问题，不是部署本身：
1. 登服务器 `curl http://127.0.0.1:【端口】`，有返回说明程序活着，问题在 nginx/域名。
2. `ping 【域名】` 出来的 IP 对不对（DNS 有没有指到你的服务器）。
3. `nginx -t` 是否通过；服务器安全组有没有放行 80 端口。
详细排查见《部署流程说明.md》服务器部署手册最后一节。

**Q：Actions 红叉了，怎么看哪一步错？**
点开那条失败记录 → 展开 `Deploy via SSH` 步骤，日志会显示卡在哪一行。常见原因：
- 连不上服务器 → 检查 `SSH_HOST` / `SSH_USER` 填对没，密钥是否装到服务器了。
- `git pull` 报权限错 → 服务器没配拉私有仓库的 Deploy key（见 4.1）。
- `npm install` 报错 → 依赖或 Node 版本问题，登服务器手动跑一遍看详细报错。

**Q：怎么确认服务在服务器上活着？**
登录服务器执行：

```bash
pm2 status              # 看【pm2进程名】是不是 online(绿色)
pm2 logs 【pm2进程名】   # 实时看这个服务的日志、报错
pm2 restart 【pm2进程名】 # 需要时手动重启
```

**Q：多人 / 多台电脑改同一个项目？**
动手前先 `git pull` 拉最新，避免和别人的改动冲突。

**Q：部署会不会把用户数据弄丢？**
不会。代码走 Git，用户数据存在服务器上（数据库/文件），两者互不影响，部署不动数据。但**一定要另外做定时备份**，防服务器整个挂掉。
